---
const contact = {
  phone: "+52 000 000 0000",
  whatsapp: "+52 000 000 0000",
  email: "ventas@talam.com",
  schedule: "L–V 9:00 a 18:00",
};
---

<section id="cotizar" class="bg-slate-50 py-20 text-slate-900">
  <div class="mx-auto grid max-w-6xl gap-12 px-6 lg:grid-cols-[1.05fr_0.95fr]">
    <div class="space-y-4">
      <div class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-3 py-1 text-xs font-semibold uppercase tracking-wide text-slate-600">
        Solicite una cotización
      </div>
      <p class="text-base leading-relaxed text-slate-600 sm:text-lg">
        Compártenos los datos básicos de tu operación y un asesor te responderá con una propuesta
        ajustada a tu ubicación, tiempos y requerimientos técnicos.
      </p>
      <div class="rounded-3xl border border-slate-200/70 bg-white p-6 text-sm text-slate-600 shadow-[0_24px_70px_-55px_rgba(15,23,42,0.35)]">
        <p class="font-semibold text-slate-900">Contacto directo</p>
        <div class="mt-3 space-y-1">
          <p>Teléfono: {contact.phone}</p>
          <p>WhatsApp: {contact.whatsapp}</p>
          <p>Correo: {contact.email}</p>
          <p>Horario: {contact.schedule}</p>
        </div>
      </div>
    </div>

    <form class="rounded-3xl border border-slate-200/70 bg-white p-6 text-slate-900 shadow-[0_28px_80px_-60px_rgba(15,23,42,0.35)]" action="#" method="post">
      <div class="grid gap-4 sm:grid-cols-2">
        <label class="text-xs font-semibold uppercase tracking-wide text-slate-600">
          Nombre / Empresa
          <input
            type="text"
            name="nombre"
            placeholder="Nombre o razón social"
            class="mt-2 w-full rounded-md border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-400 focus:outline-none"
          />
        </label>
        <label class="text-xs font-semibold uppercase tracking-wide text-slate-600">
          Teléfono / Correo
          <input
            type="text"
            name="contacto"
            placeholder="Teléfono o correo"
            class="mt-2 w-full rounded-md border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-400 focus:outline-none"
          />
        </label>
        <label class="text-xs font-semibold uppercase tracking-wide text-slate-600">
          Ubicación
          <select
            name="ubicacion"
            class="mt-2 w-full rounded-md border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-900 focus:border-blue-400 focus:outline-none"
          >
            <option>Poza Rica</option>
            <option>Veracruz</option>
            <option>Villahermosa</option>
            <option>Otra</option>
          </select>
        </label>
        <label class="text-xs font-semibold uppercase tracking-wide text-slate-600">
          Servicio requerido
          <select
            name="servicio"
            class="mt-2 w-full rounded-md border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-900 focus:border-blue-400 focus:outline-none"
          >
            <option>Renta de equipo</option>
            <option>Servicio completo</option>
          </select>
        </label>
        <label class="text-xs font-semibold uppercase tracking-wide text-slate-600 sm:col-span-2">
          Duración estimada
          <input
            type="text"
            name="duracion"
            placeholder="Ej. 2 semanas"
            class="mt-2 w-full rounded-md border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-400 focus:outline-none"
          />
        </label>
        <label class="text-xs font-semibold uppercase tracking-wide text-slate-600 sm:col-span-2">
          Comentarios / requerimientos técnicos
          <textarea
            name="comentarios"
            rows="4"
            placeholder="Describe el alcance del servicio"
            class="mt-2 w-full rounded-md border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-900 placeholder:text-slate-400 focus:border-blue-400 focus:outline-none"
          ></textarea>
        </label>
      </div>
      <div class="mt-6 flex flex-wrap gap-3">
        <button
          type="submit"
          class="rounded-md bg-blue-900 px-5 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:bg-blue-800"
        >
          Enviar solicitud
        </button>
        <a
          href={`https://wa.me/${contact.whatsapp.replace(/\D/g, "")}`}
          class="rounded-md border border-emerald-200 bg-emerald-50 px-5 py-2.5 text-sm font-semibold text-emerald-700 shadow-sm transition hover:border-emerald-300 hover:bg-emerald-100"
        >
          Cotizar por WhatsApp
        </a>
      </div>
    </form>
  </div>
</section>
